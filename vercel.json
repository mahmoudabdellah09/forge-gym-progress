{
  "version": 2,
  "installCommand": "npm install --prefix gym-progress-app",
  "buildCommand": "npm run build --prefix gym-progress-app",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    },
    {
      "src": "gym-progress-app/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/gym-progress-app/$1"
    }
  ]
}
