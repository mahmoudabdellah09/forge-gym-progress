services:
  - type: web
    name: forge-gym-progress
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PGSSLMODE
        value: require
